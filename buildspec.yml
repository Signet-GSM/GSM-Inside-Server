version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto11

  build:
    commands:
      - ./gradlew build
      - mv build/libs/*.jar app.jar
      -
artifacts:
  files:
    - app.jar
